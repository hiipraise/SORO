# ─── SORO Frontend Configuration ───
# Copy this file to .env and fill in your values

# API Base URL (Vite uses VITE_ prefix for env vars)
# In development, the Vite proxy handles /api -> backend
# In production, set this to your Render backend URL
# VITE_API_URL=https://soro-api.com
